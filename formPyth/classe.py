from student import student

class classe:
    
    def __init__(self,ID):
        self.ID = ID 
        self.nbrStudents = 0
        self.students =[]


    def add ( self , student):
        self.student.append(club)
    

    def display(self):
        for s in self.students:
            i.display()

    def delete(self,id):
        for i in self.students :
            if(i.ID == id):
                self.students.remove(i)
            

